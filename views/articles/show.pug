extends ../layout 

block content 
  br  
  h1= article.title
  h5 WritenBy #{author}
  hr
  p !{article.body}
  if user && user.id==article.author
    hr
    a.btn.btn-primary(href='/articles/'+article._id +'/edit') Edit
    |
    |
    a.btn.btn-danger.delete-article(href="#",data-id=article._id) Delete